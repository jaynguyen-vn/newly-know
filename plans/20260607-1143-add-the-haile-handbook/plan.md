# Add The Haile Handbook

## Objective
Add the `cam-nang-the-haile` handbook to the static repo flow and lightly edit the copy for a more natural Vietnamese reading experience.

## Scope
- Update the root `index.html` list so the handbook is discoverable.
- Polish visible copy in `cam-nang-the-haile/index.html`.
- Keep the existing static HTML structure and styling.

## Tasks
- [x] Inspect current repo structure and target HTML.
- [x] Add homepage entry.
- [x] Refine title, intro, footer, and awkward wording.
- [x] Run lightweight validation for syntax/readability.

## Validation
- Confirm changed files via git diff.
- Parse HTML files with a local standard-library parser.
